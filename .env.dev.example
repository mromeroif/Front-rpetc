# dev/qa = mismo front (rpetc-dev). Backend siempre productivo — ver backend/.env
# Copiar a .env.dev en la raiz del repo frontend.

FRONTEND_BUCKET=rpetc-dev
VITE_API_BASE_URL=https://tk31h2efqk.execute-api.us-east-1.amazonaws.com
